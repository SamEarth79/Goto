# URL of the deployed backend (Hugging Face Space or local dev server)
# In production, set this in your Vercel project environment variables.
# Leave empty to use the Vite dev proxy (local development).
VITE_API_URL=https://your-username-goto.hf.space
